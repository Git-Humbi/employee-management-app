function handleEdit() {
    alert("Employee record editing mode activated (Placeholder).");
}

function handleDelete() {
    const confirmation = confirm("Are you sure you want to delete?");
    if (confirmation) {
        alert("Record successfully deleted (visual confirmation).");
    }
}